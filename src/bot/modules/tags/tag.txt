set {_text} to " "
loop 5 times:
	# set {_text} to {_text} + loop-number
	add loop-number to {_text}
end
return {_text}

set {_name} to name of author from event-message
set {_id} to id of author from event-message
if {_name} is "hello":
	return "you are hello"
else if {_name} is "yes":
	return "you are yes"
else if {_name} is "hello yes":
	return "you are hello yes"
else:
	return "you aren't any"
end